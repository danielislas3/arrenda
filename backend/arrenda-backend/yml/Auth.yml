register:
  handler: src/auth/register.register
  events:
    - httpApi:
        path: /auth/register
        method: post
login:
  handler: src/auth/login.login
  events:
    - httpApi:
        path: /auth/login
        method: post
